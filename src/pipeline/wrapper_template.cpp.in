// AUTO-GENERATED by latensor — do not edit.
//
// Wrapper that replaces a polly-detected SCoP function with a call into a
// tuned TVM kernel loaded from a shared library. Linked with the original
// .o (whose original symbol has been redefined to <fn>__orig via objcopy)
// so the linker picks this strong definition.
//
// Lifetime: the TVM module + function are held in function-static slots,
// initialized on first call. The Module is intentionally leaked (allocated
// via `new`) to avoid relying on TVM runtime globals still being alive at
// process-exit static-destruction time.

#include <tvm/ffi/extra/module.h>
#include <tvm/runtime/tensor.h>

extern "C" void $function_name($cpp_signature) {
    static auto* __latensor_mod = new tvm::ffi::Module(
        tvm::ffi::Module::LoadFromFile("$lib_path"));
    static auto __latensor_fn =
        (*__latensor_mod)->GetFunction("main").value();

$dltensor_decls
    __latensor_fn($call_args);
}
